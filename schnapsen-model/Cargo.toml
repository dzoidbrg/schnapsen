[package]
name = "schnapsen-model"
version = "0.1.0"
edition = "2021"
description = "Data structures for Dreierschnapsen card game representation"
license = "GPL-3.0-or-later"
readme = "README.md"

[dependencies]
serde = { version = "1.0", optional = true }

[features]
default = []
serialization = ["serde"]
